# Dependencies
/vendor/
/node_modules/

# Build artifacts
*.zip
/dist/
/build/
# Compiled flow-builder bundle (built at package time, like vendor/).
/assets/builder/build/

# WordPress / PHP
*.log
.phpunit.result.cache
wp-cli.local.yml

# Env & secrets
.env
.env.*
!.env.example

# Agent / assistant state (never belongs in the repo)
.claude/
.omc/

# Local wp-env override (points wp-env at build/cartquill; personal preference)
.wp-env.override.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# PHPUnit
.phpunit.cache/
# PHPUnit 9 phar for the WP-integration suite (downloaded, see npm run test:integration:setup).
/phpunit-9.phar
